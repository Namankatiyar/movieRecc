[project]
name = "movierecc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NamaN", email = "7namankatiyar@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "chromadb>=1.5.9",
    "google-genai>=2.17.0",
    "python-dotenv>=1.2.2",
]

[project.scripts]
movierecc = "movierecc:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
